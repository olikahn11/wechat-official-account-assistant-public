# 更新日志

## 0.3.6 - 2026-09-25（Mac、Windows）

- 版本号放在「说明与联系」最上面，旁边有「检查新版本」。
- 有新版本时，打开软件几秒后在窗口顶部提示一条，点「去下载」打开下载页；「以后再说」这个版本不再提示。软件只提示，不会自己替换，安装还是你自己点。
- 「小黄系列」随素材更新：以后有新的小黄系列软件、小程序，打开软件就能看到，不用重装；下面有「查看全部小黄系列」。

从这一版起，以后出新版会在软件里提醒你；0.3.5 及以前的版本需要自己来下载页看。

## 0.3.5 - 2026-09-24（Mac、Windows）

包含没有单独公开发布的 0.3.3、0.3.4 的内容。

素材（两边一样）：

- 「模板」改叫「整篇排版」，「模块」「挂件」合成「样式」。样式按放在文章哪里分（标题、正文、图表流程、介绍营销、结尾引导、分割装饰），整篇排版按文章内容分；风格、颜色、会不会动收进「筛选」。
- 新增约 700 款素材：整篇排版 200 套、样式近 500 个，覆盖科技、金融、医疗、美食、旅行、教育、政务、宠物、法律、母婴、健身、非遗等 20 个行业，加上价格对比、测评打分、排期打卡、问答排障等实用工具样式。
- 只换了颜色、只换了文字的不再一条条列出，合成一张卡，卡片下面一排色点，停在色点上看那种颜色、点一下直接用。
- 两列小图卡片，一眼看到长什么样；样式在鼠标旁边显示效果，整篇排版在右边手机里显示。
- 搜索听得懂意思：可以写整句，每条结果写着为什么出现；搜索框下面显示快到的节日。第一次搜不到时问一句愿不愿意把这个词匿名发给我们补素材（随时能在「说明与联系」里关）。
- 国庆素材：整篇排版「国庆红金长文」「国庆放假通知」和 6 个国庆样式，随素材更新自动下发。
- 深色整篇排版里的正文看得清了：正文、小标题自动换成模板自己的浅色字。
- 「说明与联系」重新设计：使用说明、隐私与数据、联系我们（作者微信二维码）、小黄系列。

Windows 版：

- 看起来和 Mac 版一样：不再跟着 Windows 深色模式变成深色（想要可以在「外观」里打开）；按屏幕自动调整界面大小，高缩放的屏幕上不再像被放大了。
- 屏幕太小放不下时，右下角说明原因和办法；「外观 → 界面大小」可以自己调。
- 老电脑显卡出错时自动改用兼容模式；界面意外退出会自动重新打开。
- 素材自动更新（原来只有 Mac 有）。
- 安装前检查系统：Windows 7、8 和 32 位系统会直接说明装不了的原因。

## 0.3.2 - 2026-09-22（Mac、Windows）

Mac 版：

- 新增「我的素材」：模板、模块、挂件三个面板各有一个「我的素材」分类，放自己存的和导入的东西。
  右键任意一块「存到我的素材…」；别处复制来的样式点「粘贴新建」；可以选文件或整个文件夹一次导入很多
  （子文件夹名就是分类）；能导出成一个文件换电脑用。
- 内置素材：随软件内置模板 204 套、模块 306 个、挂件 511 个，按设计流派和行业分类。
  连同原有的，模板共 424 套、模块 576 个、挂件 561 个。
- 新增 11 种自带挂件（6 种动态、5 种静态），颜色跟随模板。
- 模块面板多了「行业分类」；挂件面板可以搜索，按行业和风格分类，原来的按用途分类放在「挂件用途」里；
  模板面板新增「经典母版」分类（16 套）。
- 素材改色：选中 HTML 块或内置挂件，可以一键把主色换成模板主色，或者逐个颜色改。
- 自己上传的模板骨架还能用 `{{digest}}`（摘要）、`{{author}}`（作者）、`{{footer}}`（页脚）、`{{keywordCTA}}`（关键词回复）。
- 修复：发布页「复制正文」粘进公众号后台，看到的是一堆 HTML 代码；现在粘进去是排好的版式。
- 修复：内置模板里的「{{date}}」没有换成日期，会原样露在文章里。
- 修复：收藏过的内置模块在「收藏」面板里看不到。

Windows 版（新版，重写，和 Mac 版一套排版引擎，排出来的文章一模一样）：

- 编辑器：大编辑区 + 右边手机实时预览；模板、模块、挂件、格式、收藏、我的素材；撤销重做、右键菜单、每一块单独调样式。
- 随软件内置和 Mac 版一样的 1021 款素材（模板 204、模块 306、挂件 511）。
- 导入 Word、PDF、Markdown、文本、HTML；文章库、我的设计、品牌设置、4 套皮肤（绘本皮肤的标题用开源的霞鹜文楷）。
- 账户与备份：多个本机账户；账户存档和 Mac 版同一格式，两边可以互相导入。
- 发布页：发布前核对标题、作者、摘要、封面，一键复制排好版的正文和发布字段，到公众号后台粘贴发布。
- 这一版还不能连接公众号；扫码连接、一键存进草稿箱在下一个版本加上。
- 和旧版「小黄公众号助手」（Windows 0.2.1）互不影响，旧版里的文章不会自动带过来。
- 安装包还没有做微软的代码签名，第一次打开会提示「Windows 已保护你的电脑」，点「更多信息 → 仍要运行」。

## 0.3.1 - 2026-09-22（Mac）

- 修复：文章里用了「HTML 自由块」或「整篇自由排版」时，保存到草稿箱的是整篇原始文字（带 `#`、`:::module` 这些符号），
  而不是排好的版式。现在和编辑器里看到的一样。
- 修复：自己导入的模板每次打开软件配色可能不一样，现在固定不变。

## 0.3.0 - 2026-09-21（Mac，未单独公开发布，内容都在 0.3.1 里）

- 软件改名为「小黄内容助手」（原「小黄公众号助手」）。
- 扫码连接公众号：通过微信官方第三方平台授权，由公众号管理员扫码确认，不需要再填写公众号开发者密码；所有公众号都能连接。
- 可选微信登录：登录后，管理员扫码授权的公众号会记到你的账号下，换电脑登录一下就能直接用。
- 模板、挂件和排版模块可以远程更新：软件打开时自动下载新素材，不用重装。首批「秋季限定」3 套模板、2 个挂件、1 个模块。
- 界面写明微信官方规定（比如只有公众号管理员能授权），避免误会成软件问题。
- 安装包里的《安装前请先读》《用户使用说明》按新流程重写，补上新版 macOS 的放行步骤。

## 0.2.2 - 2026-08-21（未公开发布）

- Mac 与 Windows 客户端统一对齐 0.2.2 版本。
- 重做 Mac 编辑器主流程、模板配方和模块/挂件系统，支持更细的文章块编辑与公众号兼容输出。
- 增加 HTML 自由块、动态模块效果、品牌资产和设计模板等能力。
- 保留微信官方 API 的权限检测、素材上传、草稿创建/更新和发布权限边界提示。
- 正式发布、部分永久链接回写和公众号高级能力仍取决于账号权限与微信认证。

## 0.2.1 - 2026-07-05

- 提供 Mac 与 Windows 客户端。
- 支持文章导入、结构识别、模板推荐与发布检查。
- 提供 22 类公众号模板和模块化内容组件。
- 支持微信官方 API 权限检测、图片素材、草稿创建/更新和永久链接同步。
- 增加本机安全凭据存储和半自动发布兜底。

已知限制：

- Mac 版尚未完成 Apple Developer ID 签名和公证。
- 微信联动能力取决于公众号权限与 IP 白名单。
- 最终发布及部分平台选项仍需在公众号后台确认。
