# 常见问题

## 软件会自动登录公众号后台吗？

不会。软件只使用微信官方 API 或官方授权能力，不保存或复用公众号后台登录态。

## 能完全一键发布吗？

不能对所有账号承诺。软件可在权限允许时创建或更新草稿；最终预览、原创、合集、话题、赞赏、群发或定时发布仍可能需要后台确认。

## 一定要填写 AppID 和 AppSecret 吗？

不用。从 0.3.0 起默认用扫码连接：公众号管理员用微信扫码授权就行，不需要填写开发者密码。
只有想用「开发者接口」直连方式时，才需要 AppID、AppSecret 和 IP 白名单（在软件的「高级」里）。

## 为什么一定要公众号管理员扫码？

这是微信官方的规定：把公众号授权给第三方工具，只能由公众号管理员扫码确认，运营者扫码不行。
管理员扫一次就够了，之后日常发文章不用再找管理员。

## 凭据保存在什么地方？

扫码授权后，微信给的授权凭据保存在我们的云服务上，用来代你调用微信官方接口；软件在这台电脑上只保存连接用的凭据
（用「开发者接口」连接的话还有你填的开发者密码），Mac 版都放在系统「钥匙串」里。凭据不会写入本公开仓库。

## 升级后为什么问我要不要允许访问钥匙串？

安装包还没有苹果的正式签名，升级到新版本后，Mac 会再确认一次新版本能不能读钥匙串里的连接凭据。
输入开机密码、点「始终允许」即可。

## 为什么 Mac 提示无法验证开发者？

当前版本还没有做苹果的正式签名和公证。请确认安装包来自本仓库 Release 或国内下载页，核对 SHA-256 后，
按 [DOWNLOAD.md](DOWNLOAD.md) 里的第 4 步在「系统设置 → 隐私与安全性」点「仍要打开」。

## 支持哪些文章文件？

支持 Markdown、TXT、HTML、DOCX 和可复制文字的 PDF，也可以直接粘贴正文。

## 公网 IP 显示仅 IPv6 怎么办？

这只和「开发者接口」连接方式（要填 IP 白名单）有关，扫码连接不受影响。可切换到提供 IPv4 的网络，检查 VPN、代理或 TUN 设置，并让微信 API 和 IPv4 检测服务走直连。

## public 仓库为什么没有源码？

这是面向用户的下载与说明仓库。完整源码和开发文档保存在独立 private 仓库。
